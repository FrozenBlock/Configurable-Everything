# Increases the amount of memory Gradle can use and optimizes the GC
	# -XX:+UseZGC
	org.gradle.jvmargs=-Xmx4G
# Helps with build times
	org.gradle.parallel=true
# Caches Dependencies for faster build times.
	org.gradle.caching=true

# mod properties
	mod_id = configurable_everything
	mod_name = Configurable Everything
	mod_author = FrozenBlock
	license = GPL-3.0
	minecraft_version_range = [26.2, 27)
	java_version = 25
	repository = FrozenBlock/Configurable-Everything

# Fabric Properties
	# Get new versions at https://fabricmc.net/develop
	minecraft_version=26.2
	fabric_loader_version=0.19.3
	min_fabric_loader_version=0.19.0

# Mod Properties
	mod_version = 2.0
	maven_group = net.frozenblock
	archives_base_name = ConfigurableEverything

# Publishing
	modrinth_project_id = 2OtUT3F2
	curseforge_project_id = 1548410

# Dependencies
	fabric_api_version=0.155.2+26.2
	fabric_kotlin_version=1.13.13+kotlin.2.4.10
	frozenlib_version=3.0-mc26.2

# External Mods
	cloth_config_version=26.2.155
	modmenu_version=20.0.1

	# Optimization
	sodium_version=mc26.1.2-0.9.1-fabric

# NeoForge Properties (verify versions against the snapshot maven)
	neoforge_version=26.2.0.72
	neoforge_loader_version_range=[4,)
    kotlinlangforge_version=2.13.0-k2.4.10-3.1+neoforge

# Optional: set to a PR/snapshot NeoForge maven URL, or leave blank to skip
	neoforge_snapshot_maven=
